import { defineConfig } from 'vite';

// Storybook-only Vite config. The react plugin is added by @storybook/react-vite;
// Tailwind comes in via the root postcss.config.js.
export default defineConfig({});
